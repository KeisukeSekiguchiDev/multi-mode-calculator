{
  "name": "multi-mode-calculator",
  "version": "1.0.0",
  "description": "Multi-Mode Calculator - Standard, Scientific, and Programmer modes",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test tests/playwright/ui.spec.js",
    "test:install": "npx playwright install chromium",
    "serve": "npx http-server src -p 8080 -c-1"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "http-server": "^14.1.1"
  }
}
